{{#tr-editor as |e|}}
    {{#e.label}}
        {{tr-error-tooltip errors=errors}}
        {{label}}
        {{tr-info-tooltip title=title content=info}}
    {{/e.label}}
    {{#if prefix}}
        {{#e.prefix}}
            {{as-html prefix}}
        {{/e.prefix}}
    {{/if}}
    {{#e.content}}
        <div class="rating-editor-content">
            <div class="rating-editor-content-fill" style="width: {{_percentage}}%;">
                <span>★★★★★</span>
            </div>
            <div class="rating-editor-content-empty">
                <span>★★★★★</span>
            </div>
        </div>
        {{yield}}
    {{/e.content}}
    {{#if postfix}}
        {{#e.postfix}}
            {{as-html postfix}}
        {{/e.postfix}}
    {{/if}}
{{/tr-editor}}
