<table class="tr-board-widget-container">
    <tr class="tr-board-widget-content">
        <td>
            {{#if contentIsEmpty}}
                <text class="tr-board-widget-content-empty-header"><tspan>{{internalChartOptions.title.text}}</tspan></text>
                <text class="tr-board-widget-content-empty-body"><tspan>Keine Daten verfügbar.</tspan></text>
            {{else}}
                {{tr-chart mode=mode chartOptions=internalChartOptions content=content theme=theme width=absWidth height=absHeight}}
            {{/if}}
        </td>
    </tr>
    <tr class="tr-board-widget-footer">
        <td>
            <p>
                {{yield}}
            </p>
        </td>
    </tr>
</table>